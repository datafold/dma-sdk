[project]
name = "dma-sdk"
version = "0.1.0"
description = "Datafold DMA SDK for SQL query translation"
requires-python = ">=3.12"
dependencies = [
    "requests>=2.32.0",
    "ipython>=8.29.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
